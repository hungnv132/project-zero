
server {
    listen 80;
    server_name 35.196.170.40 ;

    location /static {
        alias /home/ubuntu/project-zero/zero/public;
    }

    location /media {
        alias /home/ubuntu/project-zero/zero/media;
    }

    location / {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/zero.sock;
    }
}
